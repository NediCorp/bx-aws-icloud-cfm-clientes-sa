AWSTemplateFormatVersion: '2010-09-09'
Description: Ejemplo de roles de IAM para dominio de clientes

Resources:
  RolClientesIntegr:
    Type: 'AWS::IAM::Role'
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Sid: "AllowEksAuthToAssumeRoleForPodIdentity"
            Effect: "Allow"
            Principal:
              Service: "pods.eks.amazonaws.com"
            Action: "sts:AssumeRole"
      # Path: "/clientes/integr/"

  RolClientesIntcus:
    Type: 'AWS::IAM::Role'
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Sid: "AllowEksAuthToAssumeRoleForPodIdentity"
            Effect: "Allow"
            Principal:
              Service: "pods.eks.amazonaws.com"
            Action: "sts:AssumeRole"
      # Path: "/clientes/intcus/"

Outputs:
  RolClientesIntegrArn:
    Description: "ARN del Rol Clientes Integr"
    Value: !GetAtt RolClientesIntegr.Arn
    Export:
      Name: RolClientesIntegrArn

  RolClientesIntcusArn:
    Description: "ARN del Rol Clientes Intcus"
    Value: !GetAtt RolClientesIntcus.Arn
    Export:
      Name: RolClientesIntcusArn